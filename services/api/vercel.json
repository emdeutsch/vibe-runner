{
  "version": 2,
  "installCommand": "cd ../.. && npm install",
  "buildCommand": "cd ../.. && DIRECT_URL=${DIRECT_URL:-$DATABASE_URL} npx prisma migrate deploy --schema=packages/db/prisma/schema.prisma && npx turbo run build --filter=@viberunner/api",
  "outputDirectory": "dist",
  "framework": "hono"
}
